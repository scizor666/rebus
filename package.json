{
  "name": "rebus",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Browser rebus game (Soviet-style image + character puzzles).",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "crawl": "tsx tools/crawler/crawl.ts",
    "generate": "tsx tools/generator/generate.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vite": "^6.0.0",
    "vite-plugin-pwa": "^1.3.0"
  }
}
